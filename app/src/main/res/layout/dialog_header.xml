<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dialog_titleFrame"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingBottom="@dimen/dialog_title_frame_margin_bottom"
    android:paddingLeft="@dimen/dialog_frame_margin"
    android:paddingRight="@dimen/dialog_frame_margin"
    android:paddingTop="@dimen/dialog_frame_margin">

    <ImageView
        android:id="@+id/dialog_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/dialog_icon_margin"
        android:layout_marginRight="@dimen/dialog_icon_margin"
        android:scaleType="fitXY"
        android:visibility="gone"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/dialog_box_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/primary_text"
        android:textStyle="bold"
        android:textSize="@dimen/dialog_title_textsize"
        tools:text="Title" />

</LinearLayout>